import React from 'react'
export default function ProfilePerformance({ data }) {
  const max = Math.max(...data.map((item) => item.value), 1)
  return <figure className="med-card"><div className="flex flex-wrap justify-between gap-3"><figcaption className="med-heading-3">Profile performance</figcaption><span className="med-caption rounded-full bg-brand-50 px-3 py-1 text-brand-600">Last 7 days</span></div><div className="mt-8 flex h-40 items-end justify-around gap-3" aria-hidden="true">{data.map((item, index) => <div key={item.label} className="flex h-full min-w-0 flex-1 flex-col items-center justify-end gap-2"><div title={`${item.label}: ${item.value} views`} style={{ height: `${item.value / max * 100}%` }} className={`w-5 max-w-full rounded-t-md ${index === 5 ? 'bg-green-600' : 'bg-ink-200'}`} /><span className="med-caption text-ink-400">{item.label}</span></div>)}</div><table className="sr-only"><caption>Profile views over the last seven days</caption><thead><tr><th>Day</th><th>Views</th></tr></thead><tbody>{data.map((item) => <tr key={item.label}><th>{item.label}</th><td>{item.value}</td></tr>)}</tbody></table><p className="med-caption mt-5 text-ink-400">Profile views throughout the week.</p></figure>
}
